- defaults:
    link:
        create: true
        relink: true

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc

- shell:
    -
      command: powerline/fonts/install.sh
      stdout: true
      stderr: true

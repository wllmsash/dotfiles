- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.tmux/plugins/tpm: tmux/plugins/tpm
    ~/.vim: vim
    ~/.vimrc: vimrc

- shell:
    -
      command: powerline/fonts/install.sh
      stdout: true
      stderr: true
